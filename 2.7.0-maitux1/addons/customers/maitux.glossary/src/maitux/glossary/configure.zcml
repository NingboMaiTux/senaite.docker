<configure
    xmlns="http://namespaces.zope.org/zope"
    xmlns:five="http://namespaces.zope.org/five"
    xmlns:i18n="http://namespaces.zope.org/i18n"
    xmlns:genericsetup="http://namespaces.zope.org/genericsetup"
    i18n_domain="maitux.glossary">

  <i18n:registerTranslations directory="locales" />

  <include package="Products.Five" />
  <include package="Products.GenericSetup" file="meta.zcml" />
  <include package="Products.CMFCore" file="permissions.zcml" />

  <five:registerPackage package="." />

  <include package=".content" />
  <include package=".datamanagers" />
  <include package=".browser" />

  <genericsetup:registerProfile
      name="default"
      title="Maitux Keyword Glossary"
      directory="profiles/default"
      description="Inspection item keyword zh/en middle table"
      provides="Products.GenericSetup.interfaces.EXTENSION"
      post_handler=".setuphandlers.post_install"
      />

  <genericsetup:registerProfile
      name="uninstall"
      title="Maitux Keyword Glossary (uninstall)"
      directory="profiles/uninstall"
      description="Uninstalls maitux.glossary"
      provides="Products.GenericSetup.interfaces.EXTENSION"
      post_handler=".setuphandlers.uninstall"
      />

  <utility
      factory=".setuphandlers.HiddenProfiles"
      provides="Products.CMFPlone.interfaces.INonInstallable"
      name="maitux.glossary-hiddenprofiles"
      />

  <!-- ★ 词表工厂必须用 component= 注册（注册的是**函数本身**）。
       用 factory= 会让 Zope 先把函数调用一次、把得到的 SimpleVocabulary
       实例注册成工厂，`z3c.form` 解析 Choice 词表时再调用它 →
       `TypeError: 'SimpleVocabulary' object is not callable`（@@edit 500，实测）。
       同一处隐患的 SyncStates 一并改正。 -->
  <utility
      component=".vocabularies.SyncStatesVocabulary"
      provides="zope.schema.interfaces.IVocabularyFactory"
      name="maitux.glossary.vocabularies.SyncStates"
      />

  <utility
      component=".vocabularies.AcquisitionPickVocabulary"
      provides="zope.schema.interfaces.IVocabularyFactory"
      name="maitux.glossary.vocabularies.AcquisitionPick"
      />

  <utility
      component=".vocabularies.AcquisitionDecimalsVocabulary"
      provides="zope.schema.interfaces.IVocabularyFactory"
      name="maitux.glossary.vocabularies.AcquisitionDecimals"
      />

</configure>
