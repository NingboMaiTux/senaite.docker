# docker compose 会自动读取与 docker-compose.yml 同目录的 .env。
#
# 用法：
#     cp .env.example .env
#     # 然后把下面两个值改成真正的强口令
#
# .env 已在 .gitignore 里，不要提交；.env.example 只放占位值，可以提交。
# 两个变量都用 ${VAR:?...} 引用，没设置时 compose 会直接报错退出，
# 不会拿空口令把站点启起来。

# ---------------------------------------------------------------------------
# Postgres（RelStorage 后端，整个 ZODB 都在这里）
# ---------------------------------------------------------------------------
# 这一个值同时供两处使用：postgres 容器建库、以及 Zope 连库
# （buildout.cfg 的 rel-storage 口令是占位符，容器启动时由
#  docker-entrypoint.sh 按这个环境变量替换）。改一处就够了。
POSTGRES_PASSWORD=change-me-postgres

# ---------------------------------------------------------------------------
# Zope 管理员（instance / instance2 共用，写进 parts/instance/inituser）
# ---------------------------------------------------------------------------
# ⚠️ 改这个值只对**新建站点**生效。站点已经建好的话，admin 口令存在 ZODB 里，
#    要去 /acl_users 或 ZMI 改。
ZOPE_ADMIN_PASSWORD=change-me-zope-admin

# ---------------------------------------------------------------------------
# 仪器采集端的共享上报 Token（可选）
# ---------------------------------------------------------------------------
# maitux.instrument_acquisition 的 ingest 接口除了按仪器模板上登记的
# agent_token 鉴权，还留了一条共享 Token 通道兼容旧采集端。知道这个值的人
# 就能往 LIMS 灌任意检测读数，所以它是凭据，代码里不再写死。
#
# 留空 = 该通道关闭（推荐）。只有现场还有用旧值的 labgate 在跑时才需要填，
# 填的必须和采集端配置里的一致。长期方向是给每台仪器配模板 agent_token，
# 然后把这一项清空。
PHASE1_INGEST_TOKEN=

# ---------------------------------------------------------------------------
# 为什么 POSTGRES_DB / POSTGRES_USER 不在这里
# ---------------------------------------------------------------------------
# 它们在 buildout.cfg 的 rel-storage 段里是写死的（dbname lims_db / user
# lims_user）。放进 .env 会让人以为改了就生效，实际会让 Zope 连不上库。
# 要让它们真正可配置，得先改 buildout.cfg，属于另一件事。
#
# ---------------------------------------------------------------------------
# 口令一共出现在哪些地方
# ---------------------------------------------------------------------------
# 只有这个 .env 一处是真实值，其余全是占位符或引用：
#   docker-compose.yml   ${POSTGRES_PASSWORD} / ${ZOPE_ADMIN_PASSWORD}
#   buildout.cfg         __POSTGRES_PASSWORD__ / __ZOPE_ADMIN_PASSWORD__
#                        （容器启动时由 docker-entrypoint.sh 替换）
#
# 轮换口令的步骤：
#   1. 改本文件两个值
#   2. 改 Postgres 里 lims_user 的口令，要和 POSTGRES_PASSWORD 一致
#      （POSTGRES_PASSWORD 只在初始化空数据目录时建库用，库已存在时不生效）
#   3. docker compose down && docker compose up -d
#
# 注意：口令从 2026-08-11 起就在 git 历史里（旧版 docker-compose.yml 与
# buildout.cfg 的明文）。把它挪进 .env 不等于它没泄露过 —— 生产上必须换成
# 一个全新的值，而不是继续用原来那个。
